import { CheckboxControlDirective } from './checkbox-control.directive';

describe('CheckboxControlDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxControlDirective();
    expect(directive).toBeTruthy();
  });
});
